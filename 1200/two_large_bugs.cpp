#include <bits/stdc++.h>
using namespace std;

void solve() {
    all the single elements must be in the second array
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
}